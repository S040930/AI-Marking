export const DOCUMENT_INPUT_ACCEPT = 'application/pdf,.pdf,.zip';

// 与后端 MAX_DOCUMENT_SIZE_BYTES (50MB) 保持一致
export const MAX_DOCUMENT_SIZE_BYTES = 50 * 1024 * 1024;

// 与后端 submission_zip.MAX_ZIP_SIZE_BYTES 保持一致
export const MAX_ZIP_SIZE_BYTES = 150 * 1024 * 1024;
