"""失败类型分类。

后台 worker 据此决定任务的重试策略:

- 业务失败 (``BusinessError``):配置错误、文件不可识别、
  OCR 返回内容异常等确定性错误。重试无意义,应直接标记终态并结束任务,
  不进入队列的退避重试。
- 系统失败 (其余 ``Exception``):进程崩溃、数据库中断、网络瞬时抖动耗尽等。
  保留队列的租约 / 心跳 / 退避重试与死信机制。
"""

from __future__ import annotations


class BusinessError(Exception):
    """永久业务失败:配置错误、文件不可识别、返回内容异常等。

    从 OCR / 批改流水线抛出处应明确使用本类型,以与瞬时网络错误
    (仍为 ``OCRError``)区分,避免被队列无意义地重复重试三次。
    """
